#!/usr/bin/env bash
# TODO: build the randomness-beacon program and deploy it to Solana devnet.
#       Steps will include: configure solana CLI for devnet, ensure the deploy
#       keypair is funded, run `anchor build`, run `anchor deploy --provider.cluster devnet`,
#       then update Anchor.toml / .env with the resulting program ID.
