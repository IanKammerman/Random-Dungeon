{
  "alpha_source": "audit_seed",
  "alpha_epoch": 3,
  "note": "alpha for this proof = epoch 3's audit seed (SHA256(domain || \"seed\" || manifest_hash)). Generated by `cargo run -p prover --release -- --sk 12345 --alpha-hex 0x<seed>`. Locally verified with `cargo run -p verifier-client`."
}
